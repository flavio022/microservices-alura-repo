
server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/flavio022/microservices-alura-repo
          search-paths: fornecedor
